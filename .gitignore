# ── Terraform ────────────────────────────────────────────────────────────────
# Local .terraform directories (provider plugins, modules)
**/.terraform/

# Terraform state files (sensitive – managed remotely or discarded for imported output)
*.tfstate
*.tfstate.backup
*.tfstate.*.backup

# Terraform crash logs
crash.log
crash.*.log

# Terraform plan files (may contain sensitive data)
*.tfplan
*.plan.out

# Variable files with potential secrets
terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
*.auto.tfvars.json
# Keep example files
!*.tfvars.example
!*.auto.tfvars.example

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration (contains credentials)
.terraformrc
terraform.rc

# ── Terraformer ──────────────────────────────────────────────────────────────
# Terraformer working cache
.terraformer/

# Terraformer plan output directory inside generated/
generated/aws/terraformer/

# Generated temp plan files used during import (stored in /tmp but guard here too)
/tmp/plan_*.json

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
._*

# ── Editor / IDE ──────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~
.editorconfig

# ── Pre-commit ───────────────────────────────────────────────────────────────
.pre-commit-cache/

# ── Secrets / credentials ─────────────────────────────────────────────────────
*.pem
*.key
*.p12
.env
.env.*
!.env.example
